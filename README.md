# Line-Follower-Vehicle
A line follower vehicle is a small autonomous robot that uses infrared sensors and a microcontroller to track a path. It detects a white line on a dark surface and adjusts its movement in real time. Based on sensor input, it turns left, right, or moves forward, while the controller manages the motors through a driver circuit.
